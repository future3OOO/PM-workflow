# Runbook — Video Analysis & Documentation Integration (Superseded)

This file is retained only as a legacy pointer.

## Status

`RUNBOOK.md` is no longer the default workflow for this repository.

Use:

- [RUNBOOK_PERFORMANCE.md](RUNBOOK_PERFORMANCE.md)

for all new video-analysis batches.

## Why it was superseded

The performance runbook is now the default because it requires:

- denser frame review
- richer per-video evidence capture
- first-pass UI mapping with exact page-title / entry-path anchors
- action ledgers and screen / page ledgers
- stronger dependency-cascade checks
- glossary updates before batch closure

## Rule

Do not start a new batch from this file unless you are deliberately reviewing the old process for comparison purposes.
